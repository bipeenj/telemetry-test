# smart-vending-assignment

Please create a .NET 8 Web API project in this folder structure.
You may use minimal API or controller-based API.

Run: dotnet new webapi -n TelemetryApi

Create a branch name with your name such as tom_assignment

tried pushing new branch but it failed so attaching  codebas as zip.If you want have public repo under my account

P.S. For Vue UI need to remove swagger part from URL as UI is served as static files